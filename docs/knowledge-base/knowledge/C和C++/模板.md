---
title: 模板
date: 2026-08-19
category:
  - 知识库
  - C和C++
---
语法: `template <typename T>` 或者 `template <class T>` 


## 函数模板和普通函数调用规则

如果函数模板和普通函数都可以调用，会优先调用普通函数
可以通过空模板参数列表强制调用函数模板
函数模板可以发生函数重载
如果函数模板可以产生更好的匹配，优先调用函数模板

## 类模板

作用是建立一个通用类，类中的成员数据结构都可以不确定




## 类和继承

