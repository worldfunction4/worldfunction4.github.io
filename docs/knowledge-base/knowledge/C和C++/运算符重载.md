---
title: 运算符重载
date: 2026-08-19
category:
  - 知识库
  - C和C++
---
## 39 类和对象-C++运算符重载-加号运算符重载
- **核心概念**：实现自定义对象的相加（成员函数重载 vs 全局函数重载）。
- **代码示例**：
- **易错点**：通常返回新对象（传值返回），切勿返回局部对象的引用。

## 40 类和对象-C++运算符重载-左移运算符重载
- **核心概念**：配合 `cout` 实现自定义对象的打印输出。
- **代码示例**：
- **易错点**：必须写在全局作用域，不能写在类内。需返回 `ostream&` 配合链式输出。

## 41 类和对象-C++运算符重载-递增运算符重载
- **核心概念**：实现自定义对象的前置 `++` 与后置 `++`。
- **代码示例**：
- **易错点**：前置无参且返回自身引用；后置需加 `int` 占位符且必须传值返回（避免内存悬空）。

## 42 类和对象-C++运算符重载-赋值运算符重载
- **核心概念**：解决带指针成员的类在赋值时的浅拷贝奔溃问题（必须深拷贝）。
- **代码示例**：
- **易错点**：“黄金四步”：防自赋值 -> 释放旧内存 -> 深拷贝新内存 -> 返回 `*this`。

## 43 类和对象-C++运算符重载-关系运算符重载
- **核心概念**：重载 `==`、`!=` 等符号，使自定义对象支持条件判断。
- **代码示例**：
- **易错点**：返回值类型必须是 `bool`。

## 44 类和对象-C++运算符重载-函数调用运算符重载
- **核心概念**：重载 `()`，使对象使用起来像普通函数一样（即“仿函数” Functor）。
- **代码示例**：
- **易错点**：仿函数的参数表和返回值非常灵活，没有固定死规矩，常用于算法回调。