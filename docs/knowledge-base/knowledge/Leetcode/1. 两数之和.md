---
title: 1. 两数之和
date: 2026-08-19
category:
  - 算法
tag:
  - Leetcode
---
这道题其实已经写过两次了...分别是用python和c++写的，但是我还是写不出来...
问题是这样的：

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值** _`target`_  的那 **两个** 整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。

你可以按任意顺序返回答案。

**示例 1：**

**输入：**nums = [2,7,11,15], target = 9
**输出：**[0,1]
**解释：**因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。

**示例 2：**

**输入：**nums = [3,2,4], target = 6
**输出：**[1,2]

**示例 3：**

**输入：**nums = [3,3], target = 6
**输出：**[0,1]

**提示：**

- `2 <= nums.length <= 104`
- `-109 <= nums[i] <= 109`
- `-109 <= target <= 109`
- **只会存在一个有效答案**

**进阶：**你可以想出一个时间复杂度小于 `O(n2)` 的算法吗？

刚开始我想到是写一个数字对比动态数组的函数，返回一个true或者false结果，结果发现这样写有一些问题：
```cpp
class Solution {
public:
    // 用来判断一个数是否等于容器内的一个数组
    bool compare(const vector<int>& v, const int& n) {

        for (int i = 0; i < v.size(); i++) {
            if (n == v.at(i)) {
                return true;
            }
            else {
                return false;
            }
        }
    }

    vector<int> twoSum(vector<int>& nums, int target) {
        int n = 0;
        // 对每个动态数组进行操作
        for (int i = 0; i < nums.size(); i++) {
            int a = target - nums.at(i);

            if (compare(nums, a) == true) {
                for (int j = 0; j < nums.size(); j++) {
                    if (a == nums.at(j)) {
                        return {i, j};
                    }
                }
            }
        }
        return {};
    }

};
```

正确写法：
```cpp
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int, int> hashtable;
        for (int i = 0; i < nums.size(); ++i) {
            auto it = hashtable.find(target - nums[i]);
            if (it != hashtable.end()) {
                return {it->second, i};
            }
            hashtable[nums[i]] = i;
        }
        return {};
    }
};
```